{
  "name": "delux-docs",
  "version": "1.0.0",
  "private": true,
  "description": "DeLux project page dev server",
  "scripts": {
    "start": "live-server --port=8765 --open=/",
    "dev": "live-server --port=8765 --open=/ --wait=200"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
